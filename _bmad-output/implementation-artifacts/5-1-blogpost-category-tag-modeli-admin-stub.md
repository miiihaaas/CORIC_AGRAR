---
story_id: "5.1"
story-key: 5-1-blogpost-category-tag-modeli-admin-stub
title: BlogPost + Category + Tag Modeli + Admin Stub
status: ready-for-dev
epic: 5
epic_num: 5
epic_title: Blog (Priče sa polja)
module: blog
created: 2026-06-04
last_modified: 2026-06-04
complexity: H
author: Mihas (SM autonomous; PRVA story Epic 5 — Blog „Priče sa polja". INFRASTRUKTURA + MODEL FOUNDATION: uvodi NOVI app `apps/blog/` (mirror apps/forms 4-1 + apps/search 2-13 scaffolding: AppConfig + INSTALLED_APPS reg). 3 modela — `Post` (nasleđuje SluggedModel+TimestampedModel iz apps.core; FK Category SET_NULL, M2M Tag, FK author=AUTH_USER_MODEL SET_NULL, status TextChoices DRAFT/PUBLISHED, published_at, perex, body, main_image), `Category` (SluggedModel+TimestampedModel; name, description), `Tag` (SluggedModel+TimestampedModel; name) — sva tri kroz apps.core base klase + slugify_ascii. `apps/blog/translation.py` registruje translatable polja (Post.title/perex/body + Category.name/description + Tag.name → `_sr/_hu/_en` kroz modeltranslation). `apps/blog/managers.py:PublishedManager` (NOVI Blog-specific manager, NE postoji core PublishedManager) filtrira `status='published'` AND `published_at__lte=now`. `apps/blog/admin.py` STUB registracija (list_display/list_filter/search_fields + prepopulated_fields slug; PUN CRUD + WYSIWYG = Epic 8.7). `Post.get_absolute_url()` → `reverse("blog:detail", slug=...)` (raise NoReverseMatch dotle — URL je 5.2/5.3 scope; Gotcha precedent Product.get_absolute_url PR-5, SM-D6). SM-D1 PLACEMENT: NOVI `apps/blog/` (epics.md:859 „kreiram apps/blog/models.py sa Post/Category/Tag"). SM-D2 STATUS vs is_published: epics.md:864 AC eksplicitno `status='published'` + `published_at__lte=now` → Blog koristi `status` (TextChoices) kao published discriminator + `published_at`, NE products-stil dual is_published+status (PR-D4); JEDAN status field je čistiji za blog (divergence dokumentovan SM-D2). SM-D3 AUTHOR FK: `settings.AUTH_USER_MODEL` (NIKAD import User direct — project-context.md:595), `on_delete=SET_NULL` null=True (brisanje autora NE sme obrisati objavljen content). SM-D4 MIGRACIJA: SAMO schema CreateModel ×3 + M2M through-tabela (0001_initial), NEMA data seed; modeltranslation `_sr/_hu/_en` kolone za translatable polja. SCOPE BOUNDARY: 5-1 = 3 modela + translation + PublishedManager + admin stub + migracija + testovi. NEMA views/urls/pages/templates/context_processors (5.2 index, 5.3 detail, 5.4 footer; 3-1 home preview + 1-8 footer + 2-13 search „Objave" grupa konzumiraju ove modele KASNIJE). RISK TIER: HIGH — CreateModel schema migracija (×3 + M2M) + NOVI app + FK na AUTH_USER_MODEL + modeltranslation schema (prvi blog _sr/_hu/_en kolone).)
depends_on:
  - 1-2-multi-environment-settings-split-sa-django-environ  # INSTALLED_APPS (modeltranslation reg order); settings split; MODELTRANSLATION_LANGUAGES/LANGUAGES (base.py:143 — definiše _sr/_hu/_en suffixe)
  - 1-4-i18n-setup-sa-locale-url-routing-i-switcher          # i18n_patterns (blog URL će ići pod /sr/blog/ u 5.2/5.3); LANGUAGES suffix izvor; MODELTRANSLATION_FALLBACK_LANGUAGES=("sr",)
  - 2-1-brand-series-category-subcategory-modeli             # apps/core/models.py SluggedModel+TimestampedModel (REUSE base klase); apps/core/utils.py slugify_ascii (Ć→c transliteracija); save()/full_clean() slug auto-gen PATTERN; modeltranslation auto-discovery (translation.py); admin.site.register PATTERN; migrations discipline (manual review)
  - 2-2-product-i-related-modeli                             # Product PATTERN 1:1 referenca: SluggedModel+TimestampedModel nasleđe; eksplicitan on_delete+related_name; Meta.indexes `<table>_<cols>_idx` (+ _ProductIndex max_name_length gotcha za duga imena); get_absolute_url=reverse(...) raise NoReverseMatch dotle (PR-5); translation.py registracija; main_image ImageField; settings.AUTH_USER_MODEL ako bi FK na User (Product nema author — blog uvodi prvi author FK)
  - 4-1-lead-model-smtp-setup                                # NOVI-app scaffolding PATTERN (apps/forms AppConfig + INSTALLED_APPS + tests/ layout) — mirror za apps/blog; admin stub PATTERN; TimestampedModel REUSE
---

# Story 5.1: BlogPost + Category + Tag Modeli + Admin Stub

Status: ready-for-dev

## Opis

As a **dev (koji gradi blog strane u 5-2…5-4 + home preview 3-1 + footer 1-8 + search „Objave" grupu 2-13)**,

I want **NOVI `apps/blog/` app sa 3 modela (`Post`, `Category`, `Tag`), modeltranslation registracijom za translatable polja (`_sr/_hu/_en`), Blog-specific `PublishedManager` (filtrira `status='published'` + `published_at__lte=now`), STUB admin registracijom, i schema migracijom**,

so that **svi nizvodni blog konzumenti (5-2 index, 5-3 detail, 5-4 footer kolona, 3-1 home „Priče sa polja" preview, 1-8 footer „Najnovije vesti", 2-13 search „Objave" grupa) imaju JEDAN provereni model-temelj za blog sadržaj — pre nego što ijedna blog strana/view/URL bude napisana**.

Ovo je **PRVA story Epic 5 (Blog — Priče sa polja)** i čista **MODEL FOUNDATION**: TEMELJ na koji se 5-2…5-4 oslanjaju. **U OVOJ STORY NEMA NIJEDNOG VIEW-A, URL-A, NI TEMPLATE-STRANE** — to su 5-2 (blog index + paginacija + filter), 5-3 (post detail + slične objave + social share), 5-4 (footer dynamic kolona). 5-1 SAMO polaže modele + translation + manager + admin stub + migraciju.

### IN SCOPE (šta ova story isporučuje)

1. **NOVI `apps/blog/` app** (SM-D1) — AppConfig (`BlogConfig` `name="apps.blog"` + `verbose_name`) + registracija u `INSTALLED_APPS` (mirror apps/forms 4-1 + apps/products scaffolding). Drugi/treći konzument modeltranslation paketa.
2. **`Category` model** (SluggedModel + TimestampedModel) — `name` (translatable), `slug` (auto-gen iz name kroz `slugify_ascii`), `description` (translatable, blank).
3. **`Tag` model** (SluggedModel + TimestampedModel) — `name` (translatable), `slug` (auto-gen).
4. **`Post` model** (SluggedModel + TimestampedModel) — `title` (translatable), `slug` (auto-gen iz title), `perex` (translatable, kratak lead/uvod), `body` (translatable, `TextField` — NE WYSIWYG lib; rich-text editor je 8.7), `main_image` (ImageField, nullable), `category` (FK→Category `on_delete=SET_NULL` null/blank, `related_name="posts"`), `tags` (M2M→Tag `related_name="posts"` blank), `author` (FK→`settings.AUTH_USER_MODEL` `on_delete=SET_NULL` null/blank, `related_name="blog_posts"`), `status` (TextChoices DRAFT/PUBLISHED, db_index), `published_at` (DateTimeField null/blank). `objects` (default Manager) + `published` (PublishedManager). `get_absolute_url()` → `reverse("blog:detail", slug=...)`. `Meta.indexes` na `(status, -published_at)`.
5. **`apps/blog/managers.py:PublishedManager`** (SM-D5) — `get_queryset()` filtrira `status=Post.Status.PUBLISHED` AND `published_at__lte=timezone.now()` (epics.md:864). NOVI Blog-specific manager (core NEMA PublishedManager — verifikovano live).
6. **`apps/blog/translation.py`** (SM-D7) — registruje `Post` (`title`, `perex`, `body`), `Category` (`name`, `description`), `Tag` (`name`) kroz `TranslationOptions` (mirror apps/products/translation.py). Generiše `_sr/_hu/_en` virtuelna polja → DB kolone kroz makemigrations.
7. **Schema migracija** `apps/blog/migrations/0001_initial.py` (CreateModel Category + Tag + Post + M2M through-tabela `post_tags` + modeltranslation `_sr/_hu/_en` kolone — manuelno reviewovana, SM-D4). **NEMA data seed.**
8. **STUB `apps/blog/admin.py`** (SM-D8) — registruje `Post`/`Category`/`Tag` sa `list_display`/`list_filter`/`search_fields` + `prepopulated_fields={"slug": ("title",)}` (Post) / `{"slug": ("name",)}` (Category/Tag). **PUN CRUD + WYSIWYG (TinyMCE/CKEditor) + inline slike = Epic 8 Story 8.7 — NE u 5-1.**

### OUT OF SCOPE (eksplicitno — granice)

- **NIJEDAN view, URL, template-strana, context_processor, paginacija, filter, social share** — `apps/blog/views.py`, `apps/blog/urls.py`, `templates/blog/*`, `apps/blog/context_processors.py` = **Story 5-2 (blog index `/sr/blog/` + paginacija 10/strani + filter po kategoriji + kartice), 5-3 (post detail `/sr/blog/<slug>/` + slične objave + social share + kategorija/tag linkovi), 5-4 (footer `latest_blog_posts` context_processor + footer.html kolona)**. 5-1 SAMO polaže modele. (Mirror 4-1 boundary: model story NE žica view-ove; mirror 2-2 boundary: Product modeli bez view-ova.)
- **`blog:` URL namespace + `blog/urls.py`** — NE u 5-1. `Post.get_absolute_url()` vraća `reverse("blog:detail", slug=...)` koji **raise-uje `NoReverseMatch` dok 5.2/5.3 ne registruju URL** — IDENTIČAN precedent kao `Product.get_absolute_url` (Gotcha PR-5, apps/products/models.py:278-280 „URL pattern dolazi u Story 2.7 — raise-uje NoReverseMatch dotle"). NEMA praznog `urls.py` stub-a (SM-D6 — lowest-risk: prati postojeću konvenciju, ne uvodi mrtav URL pattern). epics.md:863 navodi `/blog/<slug>/` kao logički put; stvarni URL je `/sr/blog/<slug>/` kroz `i18n_patterns` (5.3 — epics.md:888).
- **PUN Blog CRUD admin sa WYSIWYG** = **Epic 8 Story 8.7** (`8-7-blog-crud-admin-sa-wysiwyg`). epics.md:862 EKSPLICITNO „full CRUD dolazi u Epic 8 8.7". 5-1 daje SAMO STUB (`list_display`/`list_filter`/`search_fields`/`prepopulated_fields`) — NEMA TinyMCE/CKEditor body editor, NEMA image upload inline preview, NEMA color picker, NEMA multi-locale tab UI (mirror 2-2 SM admin stub: bare registracija, modeltranslation tabovi se NE renderuju u stub-u; vidi Gotcha PR-11).
- **`body` rich-text/WYSIWYG model field** — NE. `body = TextField` (plain). epics.md:889 (5.3) navodi „telo (rich text sa inline slikama i video embed)" — ali rich rendering je **prezentacija/admin editor (8.7)**, NE model-field tip. Model drži raw text/HTML u `TextField`; WYSIWYG editor (admin) + safe-render (template) su 8.7/5.3 scope. NEMA `django-ckeditor`/`tinymce` dep u 5-1 (SM-D10).
- **`django-taggit` ili treća-strana tagging lib** — NE. `Tag` je CUSTOM model (epics.md:859 `Tag (name, slug)`) sa M2M `Post.tags`. NEMA `django-taggit` dep (epics.md ne traži; custom model je translatable kroz modeltranslation što taggit ne podržava out-of-box; SM-D9). NEMA novog pip/uv paketa u 5-1.
- **Home „Priče sa polja" preview (3-1)** — VEĆ shipped kao forward-compat Lorem Ipsum placeholder (`latest_posts=[]` → 2 statičke kartice + `{# EPIC-5-PLACEHOLDER #}` marker; 3-1 SM-D7). 5-1 NE dira `apps/pages` ni home template. **Story 5-4 (ili buduća 3-x revizija) popunjava queryset** koristeći `Post.published`. 5-1 SAMO obezbeđuje model.
- **Footer „Najnovije vesti" (1-8/5-4)** — footer placeholder „Uskoro nove priče" je 1-8/5-4 scope. 5-1 NE dira `templates/partials/footer.html`. 5-4 dodaje `context_processors.py:latest_blog_posts`.
- **Search „Objave" grupa (2-13)** — 2-13 SM-D3 obezbedio forward-compatible PRAZAN skelet (`grouped["objave"]=[]`). 5-1 NE dira `apps/search`. Buduća search story popunjava `objave` granu kroz `Post.published` (van scope-a 5-1).
- **SEO/SeoMeta na Post** = **Epic 6 (6.1 SeoMeta Generic FK; 6.2 sitemap BlogPost klasa)**. 5-1 NE uvodi `SeoMeta` ni meta polja na Post (5.3 koristi `Post.title`/`Post.perex` kao default meta — view-layer, NE model field; epics.md:893).
- **„Slične objave" / „featured" logika** — 5.3 scope (queryset iz iste kategorije). 5-1 NE uvodi `featured` flag ni similar-relaciju.
- **`is_published` boolean (products-stil)** — NE (SM-D2). Blog koristi JEDAN `status` field (DRAFT/PUBLISHED) + `published_at` kao published kriterijum (epics.md:864). Products PR-D4 dual is_published+status je products-specific kompromis — NE replikuje se na blog (manje polja, čistija publish semantika).
- **Defensive validacija za nemoguće slučajeve** (project-context.md:358) — NE. `clean()` SAMO ako postoji stvaran invariant (npr. PUBLISHED bez published_at — vidi AC1 napomenu; opciono, NE blokira).

### Princip

Jedan NOVI app + 3 modela (Category/Tag/Post) kroz `apps.core` base klase (SluggedModel+TimestampedModel REUSE) + slugify_ascii slug auto-gen (save()/full_clean() pattern iz 2-1/2-2) + modeltranslation registracija (translatable polja → `_sr/_hu/_en`) + Blog-specific PublishedManager (status=published AND published_at<=now) + STUB admin + jedna schema migracija (CreateModel ×3 + M2M, NEMA data seed). FK author = `settings.AUTH_USER_MODEL` (NIKAD import User direct), `on_delete=SET_NULL`. FK category = SET_NULL. M2M tags. Svi FK/M2M sa eksplicitnim `on_delete`/`related_name`. `db_index` na slug (kroz SluggedModel) + status. `Meta.indexes` imenovan `<table>_<cols>_idx`. `get_absolute_url()` = `reverse("blog:detail", ...)` (raise NoReverseMatch dotle — Gotcha PR-5). Pune dijakritike (č/ć/ž/š/đ) u verbose_name-ovima i labelama. NEMA views/urls/templates/context_processors (5-2…5-4). NEMA novog dep-a (NE django-taggit, NE WYSIWYG lib — SM-D9/D10). NEMA defensive validacije. NEMA premature abstrakcije (custom Tag model, NE taggit; jedan status field, NE dual).

### Strukturna arhitektura — repository delta

**NOVI app `apps/blog/` (8 fizičkih fajlova) + 1 INSTALLED_APPS EDIT + 0 DELETE + 0 template-strana + 0 CSS + 0 JS + 0 views/urls/context_processors.**

| Path | Tip | Razlog |
|---|---|---|
| `apps/blog/__init__.py` | NOVO | Python package marker. |
| `apps/blog/apps.py` | NOVO | `class BlogConfig(AppConfig)` `default_auto_field="django.db.models.BigAutoField"` `name="apps.blog"` (KRITIČNO — sa `apps.` prefiksom, Gotcha PR-1) + `verbose_name=_("Blog")` (mirror `apps/products/apps.py` ProductsConfig). |
| `apps/blog/models.py` | NOVO | `Category`, `Tag`, `Post` (sva 3 nasleđuju `SluggedModel, TimestampedModel` iz `apps.core.models`; import `slugify_ascii` iz `apps.core.utils`). Eksplicitan `on_delete`+`related_name` na svim FK/M2M. `save()`/`full_clean()` slug auto-gen pattern (iz Category.name / Tag.name / Post.title — mirror Product 2-2). `Post.status` nested `TextChoices` (DRAFT/PUBLISHED). `Post.published = PublishedManager()` + `objects = models.Manager()`. `Post.get_absolute_url` → `reverse("blog:detail", kwargs={"slug": self.slug})`. `Meta.indexes` imenovan (`blog_post_status_pub_idx` na `(status, -published_at)`); `gettext_lazy as _` za sve string-ove. (vidi AC1.) |
| `apps/blog/managers.py` | NOVO | `class PublishedManager(models.Manager)`: `get_queryset()` → `super().get_queryset().filter(status=Post.Status.PUBLISHED, published_at__lte=timezone.now())`. **NOVI Blog-specific** (core NEMA PublishedManager — SM-D5). Import-order safe: `Post.Status` referencira se kroz string ili lazy (vidi Gotcha BL-1). |
| `apps/blog/translation.py` | NOVO | `@register(Post)` fields=("title","perex","body"); `@register(Category)` fields=("name","description"); `@register(Tag)` fields=("name",) (mirror `apps/products/translation.py`). modeltranslation auto-discovery (apps.blog u INSTALLED_APPS POSLE `modeltranslation`). |
| `apps/blog/admin.py` | NOVO | MINIMALAN STUB koji PROLAZI `manage.py check` + changelist 200 (SM-D8). `PostAdmin(list_display=("title","category","status","published_at","author"), list_filter=("status","category","tags"), search_fields=<sr-suffiks ILI TranslationAdmin — Gotcha BL-2>, prepopulated_fields={"slug":("title",)} <ili fallback — Gotcha BL-2>, date_hierarchy="published_at", view_on_site=False)`; `CategoryAdmin(list_display=("name","slug"), search_fields=<vidi BL-2>, prepopulated_fields={"slug":("name",)})`; `TagAdmin(list_display=("name","slug"), search_fields=<vidi BL-2>, prepopulated_fields={"slug":("name",)})`. **`view_on_site=False` (PostAdmin) — sprečava 500 (Gotcha BL-5/IMP-1).** **NE „mirror products"** — realni `apps/products/admin.py` je BARE `admin.site.register(Model)` (nema šta da se mirror-uje); 5-1 radi VIŠE na fragilnoj modeltranslation površini. **NE WYSIWYG, NE inline, NE multi-locale tab UI** (8.7; Gotcha PR-11). **prepopulated_fields + search_fields gotcha sa modeltranslation — vidi Gotcha BL-2.** |
| `apps/blog/migrations/__init__.py` | NOVO | Package marker (NOVI app). |
| `apps/blog/migrations/0001_initial.py` | GENERISANO + MANUAL REVIEW | `makemigrations blog` — CreateModel Category + Tag + Post + M2M `post_tags` through-tabela + modeltranslation `_sr/_hu/_en` kolone za sva translatable polja + index `(status, -published_at)`. Dev MANUELNO reviewuje (project-context.md:221). NEMA data seed. |
| `config/settings/base.py` | EDIT | `"apps.blog"` u `INSTALLED_APPS` — POSLE `modeltranslation` (zahtev za translatable model — base.py:34 napomena) i POSLE domain app-ova (blog je samostalan content app; redosled posle products/pages OK). |
| `apps/blog/tests/*` | NOVO (TEA) | RED-phase testovi (vidi Testing). Dev NE piše testove. `apps/blog/tests/__init__.py` + `conftest.py` + test fajlovi (mirror apps/products/tests + apps/forms/tests layout). |
| `_bmad-output/implementation-artifacts/sprint-status.yaml` | EDIT | `5-1-blogpost-category-tag-modeli-admin-stub` → `ready-for-dev`; `epic-5` → `in-progress` (PRVA story epika). SM handoff tracking (NIJE deliverable). |

**NETAKNUTO (regression guards):** `apps/core/models.py` (SluggedModel+TimestampedModel REUSE — NE menja se); `apps/core/utils.py` (slugify_ascii REUSE — NE menja se); `apps/products/*`, `apps/brands/*`, `apps/search/*`, `apps/pages/*`, `apps/forms/*`, `apps/media_pipeline/*` (svi postojeći app-ovi — NE diraju se); `apps/pages` home template + `latest_posts=[]` placeholder (3-1 SM-D7 — 5-4 popunjava, NE 5-1); `templates/partials/footer.html` (1-8/5-4 footer placeholder); `apps/search` „Objave" prazna grana (2-13 SM-D3); `config/urls.py` (NEMA novih URL-ova — blog urls.py dolazi u 5-2/5-3); `pyproject.toml` (NEMA novog dep — NE taggit, NE WYSIWYG lib; SM-D9/D10); sve CSS/JS; sve postojeće migracije (dodavanje apps.blog ne sme generisati izmene u postojećim app migracijama — `makemigrations` mora dotaknuti SAMO blog/0001).

## Kriterijumi prihvatanja

**AC1 — `Category`, `Tag`, `Post` modeli u `apps/blog/models.py`; nasleđuju `SluggedModel`+`TimestampedModel`; tačna polja; eksplicitni on_delete/related_name; slug auto-gen; `system check` čist (SM-D1/D2/D3)**

- **Given** `apps/core/models.py` `SluggedModel`+`TimestampedModel` (Story 2.1, REUSE); `apps/core/utils.py` `slugify_ascii` (REUSE); Product PATTERN (2.2); NOVI `apps/blog/` app registrovan (AC5)
- **When** kreiram `Category`, `Tag`, `Post` u `apps/blog/models.py`
- **Then** **`Category(SluggedModel, TimestampedModel)`** MORA imati:
  - `name` — `CharField(max_length=200)` (**translatable** — AC4)
  - `slug` — nasleđen iz `SluggedModel` (`SlugField(max_length=140, unique=True, db_index=True)`), auto-gen iz `name` kroz `slugify_ascii` (save()/full_clean() pattern — mirror 2-1/2-2)
  - `description` — `TextField(blank=True)` (**translatable** — AC4)
  - `Meta.verbose_name=_("Kategorija")`, `verbose_name_plural=_("Kategorije")`, `ordering=["name"]`; `__str__` → `self.name`
- **And** **`Tag(SluggedModel, TimestampedModel)`** MORA imati:
  - `name` — `CharField(max_length=100)` (**translatable** — AC4)
  - `slug` — nasleđen, auto-gen iz `name`
  - `Meta.verbose_name=_("Tag")`, `verbose_name_plural=_("Tagovi")`, `ordering=["name"]`; `__str__` → `self.name`
- **And** **`Post(SluggedModel, TimestampedModel)`** MORA imati TAČNO ova polja:
  - `title` — `CharField(max_length=200)` (**translatable** — AC4)
  - `slug` — nasleđen, auto-gen iz `title` (globally unique — SluggedModel)
  - `perex` — `TextField(blank=True)` (kratak lead/uvod za kartice + meta description default; **translatable** — AC4; epics.md:874 perex u kartici, :893 meta description iz perex)
  - `body` — `TextField(blank=True)` (**translatable** — AC4; **plain TextField, NE WYSIWYG model field** — SM-D10)
  - `main_image` — `ImageField(upload_to="blog/main/", max_length=255, blank=True, null=True)` (mirror Product.main_image; epics.md:874 naslovna slika u kartici, :889 naslovna slika u detail-u)
  - `category` — `ForeignKey("blog.Category", on_delete=models.SET_NULL, related_name="posts", null=True, blank=True, verbose_name=_("Kategorija"))` (SET_NULL: brisanje kategorije NE sme obrisati objave; nullable jer post može biti bez kategorije pre dodele)
  - `tags` — `ManyToManyField("blog.Tag", related_name="posts", blank=True, verbose_name=_("Tagovi"))`
  - `author` — `ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, related_name="blog_posts", null=True, blank=True, verbose_name=_("Autor"))` (**MORA `settings.AUTH_USER_MODEL` — NIKAD import User direct, project-context.md:595; SM-D3**)
  - `status` — `CharField(max_length=12, choices=Status.choices, default=Status.DRAFT, db_index=True)` sa nested `TextChoices`:
    ```python
    class Status(models.TextChoices):
        DRAFT = "draft", _("Nacrt")
        PUBLISHED = "published", _("Objavljeno")
    ```
    **DB vrednost `"published"` je STABILAN ugovor** (PublishedManager + 5.2/5.3/5.4 query-uju `status="published"`; epics.md:864). `max_length` (12) pokriva najduži DB string `"published"` (9).
  - `published_at` — `DateTimeField(null=True, blank=True, verbose_name=_("Datum objave"))` (epics.md:864 `published_at__lte=now`; :874 datum u kartici; :889 datum u meta)
  - `objects = models.Manager()` (default — admin/sav-content pristup) + `published = PublishedManager()` (AC3; **objects MORA biti prvi/eksplicitno definisan da ostane default_manager** — vidi Gotcha BL-3)
  - `Meta.ordering=["-published_at", "-created_at"]`, `verbose_name=_("Objava")`, `verbose_name_plural=_("Objave")`, `Meta.indexes=[Index(fields=["status","-published_at"], name="blog_post_status_pub_idx")]` (imenovan `<table>_<cols>_idx`; ime 25 char ≤30 Django limit → vanilla `models.Index`, NE treba _ProductIndex subclass)
  - `__str__` → `self.title`
- **And** svi modeli imaju `save()`/`full_clean()` override sa slug auto-gen (mirror 2-1/2-2 CRIT-2 pattern: `if not self.slug and self.<source>: self.slug = slugify_ascii(self.<source>)` → `full_clean()` → `super().save()`); slug-source je `name` (Category/Tag) / `title` (Post)
- **And** svi FK/M2M imaju EKSPLICITAN `on_delete` (FK) + `related_name`; NEMA cross-app FK osim `settings.AUTH_USER_MODEL` (blog je samostalan content app; NE FK na products/brands)
- **And** `Post.get_absolute_url()` → `reverse("blog:detail", kwargs={"slug": self.slug})` (raise `NoReverseMatch` dok 5.2/5.3 ne registruju URL — Gotcha PR-5 precedent; SM-D6)
- **And** `uv run python manage.py check` exit 0; NEMA defensive validacije (project-context.md:358; opciono `clean()` ako PUBLISHED zahteva published_at — vidi napomenu, NE blokira AC)
- **Napomena (opciono, NE-blocking invariant):** ako se želi domenski invariant „PUBLISHED MORA imati `published_at`", to je legitiman `clean()` (NE defensive — to je stvarni invariant). PublishedManager ionako filtrira `published_at__lte=now` (PUBLISHED bez published_at se NE pojavljuje javno). Odluka prepuštena dev/TEA; ako se doda, mora imati test. Default: NE dodavati (manager filter pokriva runtime).
- **Napomena (slug kolizija — IMP-5):** `SluggedModel.slug` je `unique=True`, a `save()` slug auto-gen (mirror Product) NE de-duplikuje → dve objave sa istim title-om (npr. godišnja „Žetva") obe slugify-uju na istu vrednost → drugi `save()` baca `ValidationError` (full_clean → validate_unique na Python nivou PRE INSERT-a) ILI `IntegrityError` (raw INSERT). Test asertuje `(ValidationError, IntegrityError)` OR tuple (IDENTIČAN Product presedan). Ovo je KONZISTENTNO sa Product presedanom (nema de-dup) i PRIHVATLJIVO za v1 model-temelj. Slug kolizija je „editor-resolves-manually" situacija (admin `prepopulated`/editabilan slug → urednik ručno promeni). Richer auto-de-dup (suffix `-2`/`-3`) je deferred za 8.7 admin UX. **NE dodavati de-dup petlju u 5-1** (YAGNI / matches presedan).

**AC2 — Schema migracija `0001_initial` (CreateModel ×3 + M2M through + modeltranslation kolone); manuelno reviewovana; reverzibilna; NEMA data seed (SM-D4)**

- **Given** AC1, AC4 (translation registrovan); NOVI `apps/blog/` u INSTALLED_APPS (AC5)
- **When** pokrenem `uv run python manage.py makemigrations blog`
- **Then**:
  - Generiše se `apps/blog/migrations/0001_initial.py` sa `CreateModel("Category")`, `CreateModel("Tag")`, `CreateModel("Post")` + M2M `post_tags` through-tabela (auto kroz `Post.tags` M2M field) + `apps/blog/migrations/__init__.py`
  - **modeltranslation `_sr/_hu/_en` kolone su prisutne** za sva translatable polja: `title_sr/_hu/_en`, `perex_sr/_hu/_en`, `body_sr/_hu/_en` (Post); `name_sr/_hu/_en`, `description_sr/_hu/_en` (Category); `name_sr/_hu/_en` (Tag) — generisane kroz translation.py registraciju (mirror products 0001 `name_sr` itd.)
  - Migracija sadrži `models.Index(fields=["status","-published_at"], name="blog_post_status_pub_idx")`
  - **`status` kolona `max_length` MORA pokriti `"published"` (9 znakova)** — review-checklist: potvrdi `max_length>=9` (story koristi 12); kraći bi tiho odsekao/odbio `"published"` i polomio PublishedManager + 5.2/5.3 query
  - FK `author` referencira `settings.AUTH_USER_MODEL` → migracija ima `dependencies` na `migrations.swappable_dependency(settings.AUTH_USER_MODEL)` (Django auto-generiše)
  - **`makemigrations` ne sme dotaknuti nijednu POSTOJEĆU app migraciju** (samo blog/0001) — `makemigrations --check --dry-run` posle apply potvrđuje „No changes detected" za sve ostale app-ove (regression guard)
  - **NEMA data seed migracije** — blog startuje prazan (objave se kreiraju runtime kroz admin 8.7)
  - Dev MANUELNO reviewuje fajl (project-context.md:221); `migrate --plan` prikazuje plan
  - `uv run python manage.py migrate blog` primeni; `migrate blog zero` reverzuje čisto (CreateModel auto-reverzibilan)
- **And** migracija + model promene se commit-uju ZAJEDNO (atomic; project-context.md:223); NIJE editovana posle apply-a (project-context.md:226)

**AC3 — `apps/blog/managers.py:PublishedManager` filtrira `status='published'` AND `published_at__lte=now` (SM-D5)**

- **Given** AC1 (Post.status + published_at); core NEMA PublishedManager (verifikovano live)
- **When** kreiram `apps/blog/managers.py:PublishedManager` i `Post.published = PublishedManager()`
- **Then**:
  - `PublishedManager(models.Manager).get_queryset()` vraća `super().get_queryset().filter(status=Post.Status.PUBLISHED, published_at__lte=timezone.now())` (epics.md:864 — OBA uslova; `timezone.now()` iz `django.utils.timezone`, USE_TZ=True base.py:158)
  - `Post.published.all()` vraća SAMO objave sa `status="published"` I `published_at` u prošlosti/sada (buduća `published_at` NIJE u published — scheduled post pattern)
  - `Post.objects.all()` (default) vraća SVE (uključujući DRAFT + buduće published_at) — admin/sav-content pristup nepromenjen
  - DRAFT post → NIJE u `Post.published`; PUBLISHED sa `published_at=None` → NIJE u `Post.published` (jer `None__lte=now` je False/exclude); PUBLISHED sa `published_at` u budućnosti → NIJE u `Post.published`
- **And** `timezone.now()` se evaluira PER-QUERY (poziv u `get_queryset`, NE module-level konstanta — inače zamrznuto vreme; mirror brands/products get_queryset per-request pattern)
- **And** **DB-vrednost lock (IMP-3):** test asertuje `Post.Status.PUBLISHED.value == "published"` — zaključava string DB vrednost na koju se manager filter oslanja (manager koristi string literal `"published"` zbog BL-1 circular-import-safe; bez lock-a kasniji rename TextChoices vrednosti tiho razdvoji manager od modela). String literal u manageru OSTAJE (NE menja se na `Post.Status.PUBLISHED` reference) — test je guard, ne refaktor.

**AC4 — `apps/blog/translation.py` registruje translatable polja → `_sr/_hu/_en` (SM-D7)**

- **Given** AC1; modeltranslation auto-discovery (apps.blog POSLE modeltranslation u INSTALLED_APPS); LANGUAGES [sr,hu,en] (base.py:143)
- **When** kreiram `apps/blog/translation.py` (mirror `apps/products/translation.py`)
- **Then**:
  - `@register(Post)` `TranslationOptions` `fields=("title","perex","body")`
  - `@register(Category)` `TranslationOptions` `fields=("name","description")`
  - `@register(Tag)` `TranslationOptions` `fields=("name",)`
  - modeltranslation generiše virtuelna polja `title_sr/_hu/_en`, `perex_sr/_hu/_en`, `body_sr/_hu/_en`, `name_sr/_hu/_en` (Category+Tag), `description_sr/_hu/_en` → materijalizuju se kao DB kolone u 0001 (AC2)
  - Pristup `post.title` bez aktivnog language context-a → sr fallback (`MODELTRANSLATION_FALLBACK_LANGUAGES=("sr",)` base.py:152)
- **And** `uv run python manage.py check` exit 0 (translation registracija ne baca; `slug` NIJE translatable — slug ostaje jezik-neutralan ASCII, mirror products gde slug nije u TranslationOptions)

**AC5 — NOVI `apps/blog/` app scaffolding + INSTALLED_APPS registracija (SM-D1)**

- **Given** apps/forms 4-1 + apps/products NOVI-app PATTERN; modeltranslation reg order (base.py:34)
- **When** kreiram `apps/blog/` app
- **Then**:
  - `apps/blog/__init__.py` + `apps/blog/apps.py` (`class BlogConfig(AppConfig)`, `default_auto_field="django.db.models.BigAutoField"`, `name="apps.blog"`, `verbose_name=_("Blog")`) postoje (mirror `apps/products/apps.py`)
  - `"apps.blog"` dodat u `INSTALLED_APPS` (config/settings/base.py) — POSLE `modeltranslation` (KRITIČNO za translatable model; base.py:34) i POSLE domain app-ova (blog je samostalan content app)
  - blog NE importuje products/brands/search/pages/forms (samostalan; importuje SAMO `apps.core` (base klase+slugify_ascii) + Django + modeltranslation + `settings.AUTH_USER_MODEL`)
- **And** `uv run python manage.py check` exit 0; app se pojavljuje u `manage.py showmigrations blog`

**AC6 — STUB `apps/blog/admin.py` registruje Post/Category/Tag (list_display/list_filter/search_fields + prepopulated_fields slug); NIJE pun CRUD (SM-D8)**

- **Given** AC1; `apps/products/admin.py` STUB PATTERN; `apps/brands/admin.py` registracija; admin pod `i18n_patterns` (stvarni URL `/sr/admin/...`)
- **When** kreiram `apps/blog/admin.py` sa STUB ModelAdmin-ovima
- **Then**:
  - `Post`/`Category`/`Tag` registrovani na `admin.site` (pojavljuju se u admin app listi pod „Blog")
  - `PostAdmin`: `list_display=("title","category","status","published_at","author")`, `list_filter=("status","category","tags")`, `search_fields=(...)` (vidi Gotcha BL-2 — sr-suffiksovane kolone ILI TranslationAdmin ILI suženo na title; Dev bira što prolazi `check`), `prepopulated_fields={"slug":("title",)}` (vidi Gotcha BL-2 fallback), `date_hierarchy="published_at"` (project-context.md:200), **`view_on_site=False`** (KRITIČNO — IMP-1: `Post` ima `get_absolute_url` → admin renderuje „View on site" dugme čiji klik poziva `reverse("blog:detail",...)` → `NoReverseMatch` → HTTP 500 jer blog URL-ovi NE postoje do 5.2/5.3; `view_on_site=False` sprečava taj crash u 5-1 stub fazi; re-enable u 8.7/5.3 kad blog URL-ovi postoje)
  - `CategoryAdmin`: `list_display=("name","slug")`, `search_fields=("name",)`, `prepopulated_fields={"slug":("name",)}`
  - `TagAdmin`: `list_display=("name","slug")`, `search_fields=("name",)`, `prepopulated_fields={"slug":("name",)}`
  - URL-ovi se razrešavaju kroz `reverse("admin:blog_post_changelist")` itd. (admin pod `i18n_patterns` — NIKAD hardkodovan `/admin/`)
  - **`PostAdmin.view_on_site=False`** (IMP-1) — sprečava admin „View on site" dugme da pozove `Post.get_absolute_url` → `NoReverseMatch` → HTTP 500 (blog URL-ovi ne postoje do 5.2/5.3). `Category`/`Tag` nemaju `get_absolute_url` (dugme se ne renderuje) — ali ako bi ga ikad dobili, isti `view_on_site=False` važi. Re-enable u 8.7/5.3.
  - **NIJE pun CRUD:** NEMA WYSIWYG body editor, NEMA inline image, NEMA color picker, NEMA multi-locale tab UI (8.7) — dokumentovano (SM-D8; Gotcha PR-11)
  - **Admin je MINIMALAN stub koji PROLAZI `manage.py check` i renderuje changelist 200** — NE „literalni mirror products" (realni `apps/products/admin.py` je BARE `admin.site.register(Model)` bez `prepopulated_fields`/`search_fields`/`list_display` → nema šta da se mirror-uje; 5-1 admin radi VIŠE od presedana na fragilnoj modeltranslation površini — vidi Gotcha BL-2)
- **And** superuser klijent → `reverse("admin:blog_post_changelist")` / `..._category_..` / `..._tag_..` GET → HTTP 200
- **And** `prepopulated_fields` + `search_fields` sa modeltranslation — vidi Gotcha BL-2 (base `title`/`perex`/`body`/`name` postaju VIRTUELNI kad modeltranslation registruje model; realne DB kolone su `_sr/_hu/_en` → `prepopulated_fields`/`search_fields` na base imenima mogu baciti `admin.E030`/`FieldError` ILI tiho vratiti prazno; tri opcije — (a) `search_fields=("title_sr","perex_sr","body_sr")` sr-suffiksovane kolone, (b) registruj sa `TranslationAdmin` iz `modeltranslation.admin`, (c) ukloni `prepopulated_fields`/suzi `search_fields` i defer za 8.7; Dev bira što prolazi `check` + radi runtime)

## Tasks / Subtasks

> **Konvencija:** `[TEA-RED]` = Test Architect piše test PRE implementacije (mora FAIL). `[DEV-GREEN]` = Developer implementira da test prođe. **Dev NIKAD ne piše testove.** Migracije (makemigrations + MANUAL REVIEW + migrate) su DEV task-ovi (project-context.md:218-227). **NEMA `uv add`** (NE taggit, NE WYSIWYG — SM-D9/D10).

- [x] **Task 1 — NOVI `apps/blog/` app scaffolding (AC5)** `[DEV-GREEN]`
  - [x] 1.1 Kreiraj `apps/blog/__init__.py` + `apps/blog/apps.py` (`class BlogConfig(AppConfig)`, `default_auto_field`, `name="apps.blog"`, `verbose_name=_("Blog")` — mirror `apps/products/apps.py`).
  - [x] 1.2 Dodaj `"apps.blog"` u `INSTALLED_APPS` (config/settings/base.py) — POSLE `modeltranslation` (base.py:34) i POSLE domain app-ova.
  - [x] 1.3 `uv run python manage.py check` exit 0. Verifikuj: `apps/blog/` ne importuje products/brands/search/pages/forms (samostalan; samo core+Django+modeltranslation+AUTH_USER_MODEL).

- [x] **Task 2 — `Category` + `Tag` modeli (AC1)** `[DEV-GREEN]`
  - [x] 2.1 Kreiraj `apps/blog/models.py`: `Category(SluggedModel, TimestampedModel)` (`name` CharField; `description` TextField blank; slug auto-gen iz `name`); `Tag(SluggedModel, TimestampedModel)` (`name` CharField; slug auto-gen iz `name`). Import `SluggedModel`/`TimestampedModel` iz `apps.core.models`, `slugify_ascii` iz `apps.core.utils`.
  - [x] 2.2 `save()`/`full_clean()` slug auto-gen override (mirror 2-1/2-2 CRIT-2: `if not self.slug and self.name: self.slug=slugify_ascii(self.name)` → `full_clean()` → `super().save()`). `Meta.verbose_name`/`_plural` (pun dijakritik), `ordering`, `__str__`.

- [x] **Task 3 — `Post` model + nested Status (AC1)** `[DEV-GREEN]`
  - [x] 3.1 `Post(SluggedModel, TimestampedModel)` sa poljima iz AC1: `title`, `perex`, `body`, `main_image`, `category` (FK SET_NULL null/blank related_name="posts"), `tags` (M2M related_name="posts" blank), `author` (FK `settings.AUTH_USER_MODEL` SET_NULL null/blank related_name="blog_posts"), `status` (nested `TextChoices` DRAFT/PUBLISHED, db_index, default DRAFT), `published_at` (DateTimeField null/blank).
  - [x] 3.2 `objects = models.Manager()` + `published = PublishedManager()` (import iz `apps.blog.managers` — vidi Task 4; Gotcha BL-3 redosled). `get_absolute_url` → `reverse("blog:detail", kwargs={"slug": self.slug})`. `save()`/`full_clean()` slug auto-gen iz `title`.
  - [x] 3.3 `Meta.ordering=["-published_at","-created_at"]`, `verbose_name`/`_plural` (pun dijakritik), `Meta.indexes` `blog_post_status_pub_idx` na `(status,-published_at)`. `__str__` → `title`. NEMA defensive validacije (opciono PUBLISHED→published_at clean() — vidi AC1 napomenu, NE-blocking).
  - [x] 3.4 `uv run python manage.py check` exit 0.

- [x] **Task 4 — `PublishedManager` (AC3)** `[DEV-GREEN]`
  - [x] 4.1 Kreiraj `apps/blog/managers.py:class PublishedManager(models.Manager)`: `get_queryset()` → `.filter(status=..., published_at__lte=timezone.now())` (`timezone.now()` PER-QUERY u get_queryset, NE module-level; `django.utils.timezone`). Status vrednost referencira `"published"` literal ili lazy ka `Post.Status.PUBLISHED` (Gotcha BL-1 import-order: manager u zasebnom modulu uvozi/koristi Post.Status bez kružne zavisnosti — koristi string literal `"published"` ILI lazy resolve).
  - [x] 4.2 Žica `Post.published = PublishedManager()` (Task 3.2). Verifikuj `Post.objects` ostaje default (Gotcha BL-3).

- [x] **Task 5 — `translation.py` (AC4)** `[DEV-GREEN]`
  - [x] 5.1 Kreiraj `apps/blog/translation.py` (mirror `apps/products/translation.py`): `@register(Post)` fields=("title","perex","body"); `@register(Category)` fields=("name","description"); `@register(Tag)` fields=("name",). `slug` NIJE translatable.
  - [x] 5.2 `uv run python manage.py check` exit 0 (translation reg ne baca).

- [x] **Task 6 — Schema migracija (AC2)** `[DEV-GREEN]`
  - [x] 6.1 `uv run python manage.py makemigrations blog` → `0001_initial.py` (+ `migrations/__init__.py`).
  - [x] 6.2 **MANUAL REVIEW** `0001_initial.py`: CreateModel Category+Tag+Post; **operations REDOSLED — potvrdi `CreateModel("Category")` I `CreateModel("Tag")` PRE `CreateModel("Post")`** (IMP-6 — Post FK-uje Category + M2M-uje Tag → moraju postojati pre Post; Django obično ispravno ordinira ali manual review MORA potvrditi); M2M `post_tags` through; **modeltranslation `_sr/_hu/_en` kolone prisutne** (title/perex/body/name/description); index `blog_post_status_pub_idx`; `status max_length>=9`; **`migrations.swappable_dependency(settings.AUTH_USER_MODEL)` prisutan u `dependencies`** za `author` FK (IMP-6 — potvrdi eksplicitno); nasleđene slug/created_at/updated_at (project-context.md:221).
  - [x] 6.3 **REGRESSION GUARD:** `uv run python manage.py makemigrations --check --dry-run` posle commit-a modela → SAMO blog/0001 (nijedan postojeći app NE dobija novu migraciju). Ako bilo koji postojeći app dobije izmenu → STOP, istraži (apps.blog reg ne sme menjati postojeće).
  - [x] 6.4 `migrate --plan` → `migrate blog`. Verifikuj reverz: `migrate blog zero` čist. **NEMA data seed** (SM-D4).

- [x] **Task 7 — STUB `BlogAdmin` — MINIMALAN, mora PROĆI `check` + changelist 200 (AC6)** `[DEV-GREEN]`
  - [x] 7.1 Kreiraj `apps/blog/admin.py`: `@admin.register(Post)`/`(Category)`/`(Tag)` STUB ModelAdmin-ovi sa `list_display`/`list_filter`/`search_fields`/`prepopulated_fields` (slug) per AC6 (+ `date_hierarchy` na Post). **`PostAdmin.view_on_site=False`** (IMP-1 — sprečava „View on site" → `get_absolute_url` → `NoReverseMatch` → 500; vidi AC6/Gotcha BL-5). NE WYSIWYG/inline/tab UI (8.7). **NAPOMENA — NE „mirror products":** realni `apps/products/admin.py` je BARE `admin.site.register(Model)` (bez `prepopulated_fields`/`search_fields`/`list_display`) → nema presedan za mirror; 5-1 admin radi VIŠE od products na fragilnoj modeltranslation površini. Cilj task-a: MINIMALAN stub koji PROĐE `manage.py check` i renderuje changelist 200, ne literalna kopija products.
  - [x] 7.2 **Gotcha BL-2 (modeltranslation fragilnost — eksplicitno reši, NE „mirror products"):** sa modeltranslation base `title`/`perex`/`body`/`name` postaju virtuelni (realne kolone `_sr/_hu/_en`) →
    - `prepopulated_fields={"slug":("title",)}` može baciti `admin.E030` („refers to 'title' which is not in Model._meta.fields"). Opcije: (a) registruj `PostAdmin(TranslationAdmin)` iz `modeltranslation.admin` (handluje prevedena polja), (b) usmeri prepopulated na base field ako verzija modeltranslation to podržava, (c) ukloni `prepopulated_fields` i dokumentuj za 8.7 (slug auto-gen ostaje u `save()`). Dev bira što prolazi `check`.
    - `search_fields=("title","perex","body")` — base imena nisu realne DB kolone → search može `FieldError` ili tiho prazno. Opcije: (a) `search_fields=("title_sr","perex_sr","body_sr")`, (b) `TranslationAdmin`, (c) suzi na samo `title_sr` (ili izbaci body/perex) i defer richer search za 8.7. Dev bira što prolazi `check` + radi runtime.
  - [x] 7.3 Smoke: superuser otvori `reverse("admin:blog_post_changelist")` / `_category_` / `_tag_` → 200. `manage.py check` exit 0.

- [ ] **Task 8 — RED-phase testovi (AC1-AC6)** `[TEA-RED]`
  - [ ] 8.1 `apps/blog/tests/__init__.py` + `conftest.py` (mirror apps/products/tests + apps/forms/tests). `@pytest.mark.django_db` na DB testove.
  - [ ] 8.2 `test_models.py` (AC1) — sva polja + tipovi za Category/Tag/Post; nasleđe SluggedModel(slug unique)+TimestampedModel(created_at/updated_at); slug auto-gen iz name/title kroz slugify_ascii (npr. „Žetva i Đubrenje" → ASCII slug bez dijakritika); FK `category`/`author` SET_NULL + related_name; M2M `tags` related_name; `author` referencira `settings.AUTH_USER_MODEL` (NE direktan User import); `status` choices (draft/published ASCII vrednosti + pun-dijakritik labele); `Post.objects` default + `Post.published` PublishedManager prisutni; `__str__`; `Meta` verbose_name/ordering/indexes.
  - [ ] 8.3 `test_get_absolute_url.py` (AC1) — `Post.get_absolute_url()` poziva `reverse("blog:detail", ...)`; dok URL ne postoji (5.1) → `NoReverseMatch` (assert raises — Gotcha PR-5 precedent; potvrđuje da je reverse-based, NE hardkodovan string). (Kad 5.3 doda URL, ovaj test se ažurira da asertuje `/sr/blog/<slug>/`.)
  - [ ] 8.4 `test_published_manager.py` (AC3) — kreiraj Post-ove: DRAFT, PUBLISHED+published_at past, PUBLISHED+published_at=None, PUBLISHED+published_at future; `Post.published.all()` vraća SAMO PUBLISHED+past; `Post.objects.all()` vraća SVE; `timezone.now()` per-query (freeze/advance vreme ili dva poziva). **DB-vrednost lock (IMP-3):** assert `Post.Status.PUBLISHED.value == "published"` (zaključava string na koji se manager filter literal `"published"` oslanja — sprečava tihu divergenciju ako neko preimenuje TextChoices vrednost).
  - [ ] 8.5 `test_translation.py` (AC4) — `Post`/`Category`/`Tag` registrovani u modeltranslation; virtuelna polja `title_sr/_hu/_en`, `perex_*`, `body_*`, `name_*`, `description_*` postoje (introspect `_meta` ili modeltranslation `translator`); `slug` NIJE translatable; sr fallback (pristup bez language context → sr vrednost).
  - [ ] 8.6 `test_migrations.py` (AC2) — **PREFERIRAJ introspekciju 0001 operations liste (IMP-7) NAD `call_command('migrate','blog','zero')` round-trip-om u pytest sesiji** (round-trip migrate može poremetiti deljeno test-DB stanje). Introspect: `CreateModel`×3 (Category+Tag+Post, ovim redosledom — IMP-6), M2M `post_tags` through, `_sr/_hu/_en` kolone za translatable polja, index `blog_post_status_pub_idx`, `status max_length>=9`, `swappable_dependency` za author. Ako se ipak koristi round-trip `migrate blog zero` → gate-uj proper DB izolacijom (zaseban transactional test, ne deljena sesija). **REGRESSION:** `makemigrations --check` → no changes (blog/0001 finalan; nijedan drugi app nije dotaknut).
  - [ ] 8.7 `test_app_config.py` (AC5) — `BlogConfig.name=="apps.blog"`; app u INSTALLED_APPS (POSLE modeltranslation); dep boundary (apps/blog ne importuje products/brands/search/pages/forms — import-graph/AST ili modul-level assert).
  - [ ] 8.8 `test_admin.py` (AC6) — Post/Category/Tag registrovani na admin.site; `list_display`/`list_filter`/`search_fields`/`prepopulated_fields` definisani; **`PostAdmin.view_on_site is False`** (IMP-1); superuser GET `reverse("admin:blog_post_changelist")` / `_category_` / `_tag_` → 200 (NE hardkodovan put). **Smoke change-view/changelist NE sme triggerovati `Post.get_absolute_url`** (NE koristi `view_on_site`, NE renderuj „View on site" link) — inače `NoReverseMatch`/500 u 5-1 fazi (IMP-1). Po želji: eksplicitan assert da `PostAdmin` ne izlaže `view_on_site` dugme (ili da changelist sa jednim Post-om ipak vrati 200 bez reverse blog URL-a).

- [x] **Task 9 — Dev manual gate (NE pytest) (AC2/AC5)** `[DEV-GREEN]`
  - [x] 9.1 `uv run python manage.py check` + `migrate --plan` čisti; `makemigrations --check --dry-run` → no changes van blog (regression).
  - [x] 9.2 Dev shell smoke: `Category.objects.create(name="Agrotehnika")` (slug auto „agrotehnika"); `Post.objects.create(title="Žetva", status="published", published_at=timezone.now())` → `Post.published.count()==1`; DRAFT post → `Post.published.count()` nepromenjen. (Verifikovano kroz test_published_manager.py + test_models.py.)
  - [x] 9.3 Commit modeli + managers + translation + migracija + app scaffold + admin + settings ZAJEDNO (atomic; project-context.md:223). `just lint` clean (ruff check apps/blog/ čisto; preexisting non-blog ruff/test failures van scope-a).

## Dev Notes

### SM Decisions (SM-D log)

**SM-D1 — PLACEMENT: NOVI `apps/blog/` app.** epics.md:859 EKSPLICITNO „kreiram `apps/blog/models.py` sa: `Post`, `Category`, `Tag`". Scaffolding mirror-uje apps/forms (4-1 NOVI app) + apps/products (2-2): `AppConfig` (`BlogConfig` `name="apps.blog"` sa `apps.` prefiksom — Gotcha PR-1; + `verbose_name=_("Blog")` mirror ProductsConfig) + INSTALLED_APPS reg POSLE `modeltranslation` (zahtev za translatable model — base.py:34). blog je SAMOSTALAN content app: importuje SAMO `apps.core` (SluggedModel/TimestampedModel/slugify_ascii) + Django + modeltranslation + `settings.AUTH_USER_MODEL`. NE FK na products/brands (blog je nezavisan content domen).

**SM-D2 — STATUS field (NE products-stil dual is_published+status) — RECONCILIACIJA epics 'status=published'.** epics.md:859 navodi `Post(..., status, published_at, ...)` i :864 AC EKSPLICITNO „PublishedManager filtrira `status='published'` i `published_at__lte=now`". **Core NEMA `PublishableModel` ni `PublishedManager`** (verifikovano live — `apps/core/models.py` ima SAMO `TimestampedModel` + `SluggedModel`, oba `abstract=True`; project-context.md pominje PublishableModel ali NE postoji u kodu — isti nalaz kao 4-1 SM-D10). Products (2-2 PR-D4) koristi PARALELNO `is_published` (BooleanField) + `status` (TextChoices DRAFT/PUBLISHED/ARCHIVED) kao kompromis, i filtrira kroz plain `.filter(is_published=True)` u view-ovima (NEMA custom manager — `apps/products/managers.py` NE postoji; bio deferred u 2-2 ka 2-6 ali nije materijalizovan). **ODLUKA za blog (locked):** Blog koristi JEDAN `status` field (TextChoices DRAFT/PUBLISHED — bez ARCHIVED, blog nema arhivu u v1) + `published_at`, TAČNO kako epics.md:864 AC traži (`status='published'`). NE dodaje se redundantan `is_published` boolean — epics.md za blog NE traži dual field; jedan `status` + manager je čistija publish semantika i izbegava sync-problem (products PR-D4 priznao da is_published/status mogu da se raziđu). **Divergence od products dokumentovan:** products = dual (PR-D4 legacy kompromis); blog = single status (epics.md-driven, cleaner). Oba su validna; blog ne nasleđuje products-ov dual-field dug.

**SM-D3 — AUTHOR FK = `settings.AUTH_USER_MODEL`, `on_delete=SET_NULL`.** epics.md:859 `Post(..., FK author, ...)`. **MORA `settings.AUTH_USER_MODEL`** (NIKAD `from django.contrib.auth.models import User` — project-context.md:595 anti-pattern; omogućava custom User u Epic 8 RBAC 8.2 bez migracionog loma). **`on_delete=SET_NULL` + `null=True, blank=True` (JUSTIFY):** brisanje admin/editor user-a (Epic 8.2 RBAC superadmin/editor groups — useri se mogu deaktivirati/obrisati) NE sme kaskadno obrisati OBJAVLJENE blog objave (content integritet > author referenca). `SET_NULL` ostavlja objavu bez autora (admin re-dodeli); `PROTECT` bi blokirao brisanje user-a sa objavama (loš UX za Epic 8.2 user management); `CASCADE` bi obrisao content (neprihvatljivo). `related_name="blog_posts"` (NE „posts" — izbegava clash sa potencijalnim drugim author-related; čitljivo `user.blog_posts`). `blank=True` jer admin može sačuvati draft bez autora.

**SM-D4 — MIGRACIJA: SAMO schema (0001), NEMA data seed.** Za razliku od 3-4 SiteSettings (singleton seed) ili 2-10/2-12 (brand/category seed), blog startuje PRAZAN — objave se kreiraju runtime kroz admin (8.7). → SAMO `0001_initial` CreateModel ×3 + M2M `post_tags` through-tabela (auto kroz `Post.tags`) + modeltranslation `_sr/_hu/_en` kolone. Index `(status, -published_at)` jer PublishedManager + 5.2 listing query-uju `status=published ORDER BY -published_at` — composite index ubrzava. `author` FK → migracija dobija `swappable_dependency(settings.AUTH_USER_MODEL)` (Django auto). Manual review (project-context.md:221). REGRESSION: makemigrations sme dotaknuti SAMO blog/0001 (Task 6.3 guard).

**SM-D5 — `PublishedManager` = NOVI Blog-specific (NE reuse core — core ga NEMA).** epics.md:864 „PublishedManager filtrira `status='published'` i `published_at__lte=now`". **Core NEMA PublishedManager** (verifikovano live — vidi SM-D2). Products takođe NEMA materijalizovan PublishedManager (filtrira inline u view get_queryset). → 5-1 uvodi `apps/blog/managers.py:PublishedManager(models.Manager)` `get_queryset()` → `.filter(status=Post.Status.PUBLISHED, published_at__lte=timezone.now())`. **OBA uslova (AND)** — PUBLISHED status NIJE dovoljan ako `published_at` nije postavljen/u budućnosti (scheduled-publish pattern: postavi PUBLISHED+future published_at → objava se „pojavi" tek kad now() prestigne published_at, bez cron-a). `timezone.now()` u `get_queryset` (PER-QUERY, NE module-level — inače zamrznuto vreme; mirror brands/products get_queryset per-request). `Post.objects=models.Manager()` ostaje default za admin (sav content). Gotcha BL-1 (import order) + BL-3 (manager redosled) — vidi Gotchas.

**SM-D6 — `get_absolute_url` = `reverse("blog:detail", ...)`, raise NoReverseMatch dotle (NE url stub sada).** epics.md:863 „`Post.get_absolute_url()` vraća `/blog/<slug>/`". **PRECEDENT (locked):** `Product.get_absolute_url` (apps/products/models.py:278-280) vraća `reverse("products:detail", kwargs={"slug": ...})` i EKSPLICITNO „URL pattern dolazi u Story 2.7 — raise-uje NoReverseMatch dotle (Gotcha PR-5)". 5-1 prati IDENTIČAN obrazac: `reverse("blog:detail", kwargs={"slug": self.slug})`. **LOWEST-RISK ANALIZA (zašto NE prazan urls.py stub sada):** (a) prati postojeću, dokazanu konvenciju (products) — konzistentnost; (b) prazan `blog/urls.py` sa registrovanim `name="detail"` ali bez view-a zahtevao bi placeholder view (mrtav kod) ILI bi `include` praznog urlpatterns bio no-op koji i dalje ne rešava reverse → nema dobit; (c) 5.2/5.3 su VLASNICI URL-a (`blog:detail`, `blog:index`, `blog:category`, `blog:tag` — epics.md:877/888/891/892) i registruju ga sa stvarnim view-om + `app_name="blog"`; 5-1 NE pretpostavlja njihov URL signature. **epics.md `/blog/<slug>/` vs stvarni `/sr/blog/<slug>/`:** logički put je `/blog/<slug>/`; kroz `i18n_patterns` (config/urls.py:25) stvarni URL dobija locale prefiks `/sr/blog/<slug>/` (epics.md:888 to potvrđuje za 5.3) — `reverse("blog:detail")` automatski daje aktivni-locale prefiks. 5-1 NE registruje URL; `get_absolute_url` je reverse-based ugovor koji 5.3 aktivira. TEA test (8.3) asertuje `NoReverseMatch` u 5-1 (potvrda reverse-based, NE hardkodovan); 5.3 ažurira test da asertuje stvarni put.

**SM-D7 — TRANSLATION SCOPE: Post(title/perex/body) + Category(name/description) + Tag(name).** epics.md:860 „`apps/blog/translation.py` za title, perex, body, Category.name/description, Tag.name". Mirror `apps/products/translation.py` (auto-discovery; `@register` + `TranslationOptions.fields`). **`slug` NIJE translatable** (mirror products — slug je ASCII jezik-neutralan; lokalizovani slug-ovi su Epic 6 6.6 scope; jedan globalno-unique slug po objavi). modeltranslation generiše `_sr/_hu/_en` virtuelna polja → DB kolone u 0001. sr fallback kroz `MODELTRANSLATION_FALLBACK_LANGUAGES=("sr",)` (base.py:152). `body` je translatable TextField (plain — NE WYSIWYG; SM-D10).

**SM-D8 — ADMIN STUB = MINIMALAN, mora PROĆI `check` + changelist 200 (NE „mirror products"; NE pun CRUD/WYSIWYG = 8.7).** epics.md:862 EKSPLICITNO „Inicijalni `apps/blog/admin.py` registruje Post/Category/Tag (full CRUD dolazi u Epic 8 8.7)". **KOREKCIJA presedana (IMP-2):** raniji opis „mirror `apps/products/admin.py` STUB obrazac" je POGREŠAN — realni `apps/products/admin.py` je BARE `admin.site.register(Model)` BEZ `prepopulated_fields`/`search_fields`/`list_display` (verifikovano live). Dakle 5-1 admin radi VIŠE od presedana, na fragilnoj modeltranslation površini → cilj je MINIMALAN stub koji PROĐE `manage.py check` i renderuje changelist 200, NE literalna kopija products. 5-1 daje `list_display`/`list_filter`/`search_fields` (project-context.md:200) + `prepopulated_fields={"slug":(...)}` — ALI obe su fragilne sa modeltranslation (base `title`/`name`/`perex`/`body` postaju virtuelni; realne kolone `_sr/_hu/_en`): `prepopulated_fields` → moguć `admin.E030`; `search_fields` → moguć `FieldError`/tiho prazno. Tri rešenja po opciji (vidi Gotcha BL-2): TranslationAdmin (`modeltranslation.admin`), sr-suffiksovane kolone (`title_sr` itd.), ili ukloni/suzi i defer za 8.7. Dev bira što prolazi `check` + radi runtime. **`PostAdmin.view_on_site=False`** (Gotcha BL-5/IMP-1) — `Post` ima `get_absolute_url` → „View on site" dugme bi pozvalo `reverse("blog:detail",...)` → `NoReverseMatch` → 500 (blog URL ne postoji do 5.2/5.3); re-enable u 8.7/5.3. **Epic 8 Story 8.7 (`8-7-blog-crud-admin-sa-wysiwyg`) dodaje:** WYSIWYG body editor (TinyMCE/CKEditor), inline image upload + preview, multi-locale tab UI (TranslationAdmin), publish workflow, re-enable `view_on_site`. 5-1 STUB je za inicijalni admin pregled/unos, NE production content authoring.

**SM-D9 — `Tag` je CUSTOM model (NE django-taggit).** epics.md:859 `Tag (name, slug)` — eksplicitan custom model sa M2M `Post.tags`. **NE django-taggit (NEMA `uv add`):** (a) epics.md ne traži taggit; (b) Tag mora biti **translatable** (`name` → `_sr/_hu/_en`, epics.md:860) što taggit out-of-box ne podržava (taggit Tag nije modeltranslation-aware bez custom through); (c) custom Tag + M2M je trivijalan i pod našom kontrolom (slug auto-gen, admin stub, translation). → 5-1 NE dodaje nijedan tagging dep.

**SM-D10 — `body` = plain `TextField` (NE WYSIWYG model field; NEMA `uv add`).** epics.md:859 `body` (bez tipa); epics.md:889 (5.3) „telo (rich text sa inline slikama i video embed)" — ali to je **rendering/admin-editor karakteristika (8.7 WYSIWYG + 5.3 safe-render template)**, NE model field tip. Model drži raw text/HTML u `TextField(blank=True)`. WYSIWYG editor (`django-ckeditor`/`django-tinymce`) je admin dependency koju uvodi **Epic 8 8.7** (`8-7-blog-crud-admin-sa-wysiwyg`); 5.3 detail strana radi safe-render (`|safe` sa sanitizacijom ili structured render). 5-1 NE dodaje WYSIWYG dep, NE postavlja editor widget. `body` je translatable (SM-D7). (NAPOMENA: ako 8.7 odabere structured/block content umesto raw HTML, `body` TextField i dalje služi — block JSON bi bio zaseban field/model, NE blokira 5-1.)

### Gotchas (blog-specific traps)

**Gotcha BL-1 — `PublishedManager` import order (manager u zasebnom modulu referencira `Post.Status`).** `apps/blog/managers.py` definiše `PublishedManager` koji filtrira `status=Post.Status.PUBLISHED`. Ali `apps/blog/models.py` importuje `PublishedManager` iz `managers.py` (`Post.published = PublishedManager()`) → potencijalna kružna zavisnost (managers→models→managers). **REŠENJE:** manager filtrira kroz **string literal `status="published"`** (DB vrednost je stabilan ugovor — SM-D2) ILI `get_queryset` lazy-referencira (`self.model.Status.PUBLISHED` unutar metode, ne na import-time). Preferiraj string literal `"published"` (jednostavno, bez circular import; DB vrednost je locked) ILI `self.model` lazy pristup. NE importuj `Post` na vrhu `managers.py`.

**Gotcha BL-2 — `prepopulated_fields` + `search_fields` + modeltranslation (admin je fragilan; NE „mirror products" — products admin je BARE register bez ovih opcija).** Kad modeltranslation registruje model, base polja (`title`/`perex`/`body`/`name`/`description`) postaju VIRTUELNA (proxy); realne `_meta.fields` DB kolone su `_sr/_hu/_en`. Dve konkretne fragilnosti:
- **(a) `prepopulated_fields={"slug":("title",)}`** referencira BASE `title` koji nije u `Model._meta.fields` → moguć system check **`admin.E030`** („refers to 'title' which is not in Model._meta.fields") ILI prepopulate JS ne radi. **Tri opcije (Dev bira što prolazi `check`):** (1) registruj admin sa `TranslationAdmin` (`from modeltranslation.admin import TranslationAdmin`) → prevedena polja se handluju; (2) usmeri prepopulated na base field ako verzija modeltranslation to podržava; (3) **fallback** — ukloni `prepopulated_fields` iz stub-a (slug se i dalje auto-gen-uje u `save()` — model-level; admin prepopulate je samo JS convenience) i dokumentuj za 8.7 (pun TranslationAdmin rešava).
- **(b) `search_fields=("title","perex","body")`** — isti problem: base imena nisu realne DB kolone → admin search može baciti **`FieldError`** ILI tiho vratiti prazno. **Opcije:** (1) koristi sr-suffiksovane kolone `search_fields=("title_sr","perex_sr","body_sr")` u stub-u; (2) `TranslationAdmin`; (3) izbaci `body`/`perex` iz `search_fields` (ili suzi na samo `title_sr`) i defer richer search za 8.7. Dev bira što prolazi `check` + radi runtime.
NE blokira story (slug auto-gen je u modelu, AC1; admin je MINIMALAN stub). Verifikuj na `check` (Task 7.2/7.3).

**Gotcha BL-5 — `PostAdmin.view_on_site=False` (sprečava real 500 u 5-1 fazi).** Django admin renderuje „View on site" dugme kad model definiše `get_absolute_url`. `Post` ga ima → klik poziva `get_absolute_url` → `reverse("blog:detail", ...)` → `NoReverseMatch` → **HTTP 500** (blog URL-ovi NE postoje do 5.2/5.3). **REŠENJE:** `PostAdmin.view_on_site = False` u stub-u. `Category`/`Tag` nemaju `get_absolute_url` (dugme se ne renderuje) — ali isti princip važi ako ga ikad dobiju. Re-enable u 8.7/5.3 kad blog URL-ovi postoje. Test (8.8): admin smoke NE sme triggerovati `get_absolute_url` (NE koristi `view_on_site`).

**Gotcha BL-3 — Manager redosled (`objects` mora ostati default).** Django: PRVI definisan manager je `_default_manager`. Ako se `published = PublishedManager()` definiše PRE `objects = models.Manager()`, onda `Post._default_manager` postaje `published` (filtrira!) → admin/migracije/related lookups bi videli SAMO published objave (BUG). **REŠENJE:** definiši `objects = models.Manager()` PRVI (eksplicitno), `published = PublishedManager()` DRUGI. Tako `objects` ostaje default_manager (admin/relacije vide sav content), `published` je eksplicitan opt-in za javne view-ove (5.2/5.4). Test (8.4) verifikuje `Post._default_manager` vraća sav content.

**Gotcha BL-4 — Index ime dužina (`blog_post_status_pub_idx` = 25 ≤30).** Za razliku od products (2-2 `_ProductIndex` subclass sa `max_name_length=64` jer su imena 32-38 char), blog index ime `blog_post_status_pub_idx` je 25 char ≤ Django default `Index.max_name_length=30` → koristi VANILLA `models.Index` (NE treba _ProductIndex subclass). Ako se doda drugi duži index u budućnosti → razmotri subclass. Za 5-1: vanilla `models.Index` dovoljno.

### Project Structure Notes

- `apps/blog/` je NOVI app (mirror apps/forms 4-1 + apps/products 2-2): `__init__.py`, `apps.py` (BlogConfig), `models.py`, `managers.py`, `translation.py`, `admin.py`, `migrations/`, `tests/`. **NEMA `views.py`/`urls.py`/`context_processors.py`/`forms.py` u 5-1** (views/urls = 5-2/5-3; context_processor = 5-4).
- Base klase REUSE: `SluggedModel` (slug globally unique, db_index — sva 3 modela; mirror Product koji takođe nasleđuje SluggedModel) + `TimestampedModel` (created_at/updated_at — epics.md:859 `created_at` POKRIVEN nasleđem). slug auto-gen kroz `slugify_ascii` (apps/core/utils — Ć/Č/Š/Ž/Đ → ASCII; save()/full_clean() CRIT-2 pattern iz 2-1/2-2).
- modeltranslation: `apps.blog` u INSTALLED_APPS POSLE `modeltranslation` (base.py:34 KRITIČNA napomena — modeltranslation pre admin); `translation.py` auto-discovery. `_sr/_hu/_en` kolone generisane u 0001 (mirror products 0001 `name_sr` itd.). LANGUAGES [sr,hu,en] (base.py:143) definiše suffixe.
- `author` FK = `settings.AUTH_USER_MODEL` (PRVI author FK u projektu — Product/Brand/Lead nemaju user FK). Migracija dobija `swappable_dependency`. NIKAD direktan `User` import (project-context.md:595).
- `get_absolute_url` reverse-based (`blog:detail`) raise NoReverseMatch do 5.2/5.3 (Gotcha PR-5; mirror Product). URL je `/sr/blog/<slug>/` kroz i18n_patterns (5.3).
- **FORWARD-DEP konzumenti (NE diraju se u 5-1, samo dobijaju model):** 5-2 (blog index — `Post.published` listing + paginacija + Category filter), 5-3 (detail — `Post` + slične objave iz iste Category + Tag/Category linkovi + social share), 5-4 (footer `latest_blog_posts` = `Post.published.order_by(<eksplicitno>)[:3]` — **MORA eksplicitan `.order_by(...)`, NE oslanjanje na `Meta.ordering`; redosled `created_at` vs `published_at` je OQ-5 — IMP-4**), 3-1 (home „Priče sa polja" — popunjava `latest_posts` placeholder, 3-1 SM-D7), 1-8 (footer „Najnovije vesti" placeholder), 2-13 (search „Objave" prazna grana, 2-13 SM-D3). Svi konzumiraju `Post.published` / `Category` / `Tag` KASNIJE.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-5.1] (`:852-864`) — Post(slug/title/perex/body/main_image/FK Category/M2M Tag/FK author/status/published_at/created_at), Category(name/slug/description), Tag(name/slug); translation.py (title/perex/body + Category.name/description + Tag.name); migration `_sr/_hu/_en`; admin stub (full CRUD = 8.7); `Post.get_absolute_url` → `/blog/<slug>/`; PublishedManager `status='published'` + `published_at__lte=now`
- [Source: _bmad-output/planning-artifacts/epics.md#Story-5.2..5.4] (`:866-906`) — downstream konzumenti: 5.2 (`/sr/blog/`, kartice naslovna/datum/naslov/perex, paginacija 10, Category filter, `/sr/blog/<slug>/` link, empty state), 5.3 (`/sr/blog/<slug>/`, naslovna/datum+autor+kategorija meta, body rich-text+video, slične objave iz Category, social share, `/sr/blog/kategorija/<slug>/` + `/sr/blog/tag/<slug>/`, meta iz title+perex), 5.4 (`latest_blog_posts` context_processor 3 najnovije po created_at, footer kolona)
- [Source: _bmad-output/planning-artifacts/epics.md#Story-8.7] (`:146` sprint-status; epics „full CRUD Epic 8 8.7") — Blog CRUD admin sa WYSIWYG = Epic 8 boundary (SM-D8/D10)
- [Source: _bmad-output/planning-artifacts/epics.md#Story-3.1] — home „Priče sa polja" `latest_posts=[]` Lorem Ipsum placeholder (3-1 SM-D7); 5-1 obezbeđuje model, 5-4/3-x popunjava
- [Source: apps/core/models.py] — `SluggedModel` (slug max_length=140 unique db_index) + `TimestampedModel` (created_at/updated_at); oba `abstract=True`. **NEMA PublishableModel ni PublishedManager** (reconciliacija SM-D2/D5)
- [Source: apps/core/utils.py] — `slugify_ascii` (SR_DIGRAPHS Dž/Lj/Nj + SR_DIAKRITICI Ć/Č/Š/Ž/Đ → ASCII; slug auto-gen helper)
- [Source: apps/products/models.py] — Product PATTERN 1:1: `Product(SluggedModel, TimestampedModel)` :80; `StatusChoice` TextChoices DRAFT/PUBLISHED/ARCHIVED :95; main_image ImageField :135; eksplicitan on_delete+related_name :100-121; Meta.indexes imenovan + _ProductIndex max_name_length gotcha :56-72/183-199; save()/full_clean() slug auto-gen :253-276; `get_absolute_url` → reverse raise NoReverseMatch :278-280 (Gotcha PR-5 precedent, SM-D6)
- [Source: apps/products/translation.py] — modeltranslation `@register`+`TranslationOptions.fields` PATTERN (mirror za blog translation.py); slug NIJE u fields (SM-D7)
- [Source: apps/products/admin.py] — STUB `admin.site.register` PATTERN (modeltranslation tabovi NE u stub-u; pun CRUD = 8.x; Gotcha PR-11; SM-D8)
- [Source: apps/forms/apps.py + apps/products/apps.py] — NOVI-app AppConfig PATTERN (`name="apps.<x>"` sa prefiksom Gotcha PR-1; ProductsConfig `verbose_name=_(...)`)
- [Source: config/settings/base.py:28-52] — INSTALLED_APPS (modeltranslation PRE admin :34; dodati apps.blog POSLE modeltranslation+domain app-ova); :143-152 LANGUAGES [sr/hu/en] + MODELTRANSLATION_FALLBACK_LANGUAGES=("sr",) (suffix izvor + fallback)
- [Source: config/urls.py:25-33] — `i18n_patterns` (blog URL će dobiti `/sr/` prefiks u 5.2/5.3; `reverse("blog:detail")` daje locale prefiks); blog NIJE include-ovan u 5-1
- [Source: _bmad-output/project-context.md] — :595 NIKAD import User direct (settings.AUTH_USER_MODEL — SM-D3); :148-158 model base/FK on_delete/related_name/no get_absolute_url-na-config/reverse; :200-201 admin list_display/list_filter/search_fields; :218-227 migrations discipline (makemigrations→manual review→migrate; commit zajedno); :357-358 YAGNI/no defensive validacija; :154 modeltranslation za content
- [Source: apps/products/views.py:105 + apps/brands/views.py:75] — `.filter(is_published=True)` inline (products NEMA PublishedManager — kontrast za SM-D2/D5; blog UVODI manager)
- [Source: _bmad-output/implementation-artifacts/4-1-lead-model-smtp-setup.md] — NOVI-app scaffolding + admin stub + TimestampedModel REUSE + migration discipline PRECEDENT (prethodna NOVI-app story)

### Open Questions (OQ)

- **OQ-1 (URL signature za `blog:detail` — 5.2/5.3 vlasnik):** 5-1 `get_absolute_url` pretpostavlja `reverse("blog:detail", kwargs={"slug": ...})`. 5.2/5.3 registruju `app_name="blog"` + `name="detail"` (+ `index`/`category`/`tag` — epics.md:877/891/892). Ako 5.2/5.3 odaberu drugačiji name/kwargs → ažurirati `get_absolute_url` + test 8.3. Default ugovor: `blog:detail` sa `slug` kwarg (mirror products:detail). NE blokira 5-1 (raise NoReverseMatch je očekivan dotle).
- **OQ-2 (PUBLISHED→published_at invariant — clean()?):** Da li forsirati da PUBLISHED post MORA imati `published_at` (clean() ValidationError)? PublishedManager ionako filtrira `published_at__lte=now` (PUBLISHED bez published_at se NE pojavljuje javno — graceful). Argument ZA clean(): admin UX (ne dozvoli „published" bez datuma). Argument PROTIV: admin može hteti „published draft" (status PUBLISHED ali published_at kasnije = scheduled). Default v1: NE clean() (manager filter pokriva; scheduled-publish je feature). Ako 8.7 admin workflow traži strict → dodati u 8.7. NE blokira 5-1.
- **OQ-3 (lokalizovani slug — Epic 6 6.6):** slug je jedan globalno-unique ASCII (NE translatable — SM-D7). epics.md:891 5.3 koristi `/sr/blog/kategorija/<slug>/` + `/sr/blog/tag/<slug>/`. Lokalizovani slug-ovi (npr. `/hu/blog/<madjarski-slug>/`) su Epic 6 6.6 scope (`seo-hreflang-html-tagovi-locale-aware-slug-ovi`). v1: jedan ASCII slug po objavi/kategoriji/tag-u, isti za sve locale. NE blokira 5-1.
- **OQ-4 (`body` storage format — raw HTML vs block — 8.7):** 5-1 `body` je plain TextField (raw text/HTML). 8.7 WYSIWYG odlučuje editor (TinyMCE/CKEditor → raw HTML) + 5.3 safe-render strategiju (sanitizacija). Ako 8.7 odabere block/structured content (JSON) → može zahtevati zaseban field/model (NE menja 5-1 TextField nužno — block bi bio dodatni field). Default: TextField raw. Sanitizacija/XSS-safe render = 5.3/8.7 (5-1 NE renderuje body). NE blokira 5-1.
- **OQ-5 (5.4 footer „Najnovije vesti" ordering — `created_at` vs `published_at` — 5.4 + product confirm; IMP-4):** epics.md:288/902 kaže da footer „Najnovije vesti" prikazuje 3 najnovije objave po **`created_at`**, dok model `Meta.ordering = ["-published_at","-created_at"]` i Dev Notes referencira `Post.published[:3]` (sortira po `published_at`). **Te dve uputnice se NE slažu.** Schema služi OBE (i `created_at` i `published_at` postoje) → **NEMA re-migracije u 5-1**. ALI: **5.4 context_processor MORA SVESNO izabrati redosled kroz EKSPLICITAN `.order_by(...)` — NE sme se osloniti na `Meta.ordering`** (koje je `-published_at`, ne `-created_at`). Preporuka: `.order_by("-published_at")` za „latest news" footer (izbegava da sveže-kreirana-ali-buduće-zakazana objava iskoči pre svog datuma) — ALI epics.md kaže `created_at`. 5.4 odlučuje + product potvrđuje. NE blokira 5-1 (model služi oba polja).
- **OQ-6 (project-context.md stale `PublishableModel` referenca — deferred doc-fix; IMP-8):** `_bmad-output/project-context.md` (~:105) navodi „PublishableModel" kao core base klasu, ALI ona NE postoji u `apps/core/models.py` (core ima SAMO `TimestampedModel` + `SluggedModel` — verifikovano live; isti nalaz kao SM-D2/D5). Stale doc referenca koja može zavesti BUDUĆE story-je/agente. **Out of scope za OVU story** — NE editovati `project-context.md` iz ovog task-a; flag-ovano kao zaseban deferred doc-pass (sibling Epic 4 OQ-5 project-context.md `block=True` doc problema). 5-1 svesno koristi SAMO TimestampedModel+SluggedModel (ne PublishableModel).

### Testing notes (šta TEA pokriva — RED phase)

- **Modeli (AC1):** Category/Tag/Post sva polja + tipovi; nasleđe SluggedModel(slug max_length=140 unique db_index)+TimestampedModel(created_at/updated_at); slug auto-gen iz name/title kroz slugify_ascii (dijakritik → ASCII, npr. „Đubrenje" → „dubrenje"); `category`/`author` FK SET_NULL + null/blank + related_name; `tags` M2M related_name; **`author` referencira `settings.AUTH_USER_MODEL`** (NE direktan User import — introspect `Post._meta.get_field("author").related_model == get_user_model()`); `status` TextChoices (draft/published ASCII vrednosti + pun-dijakritik labele Nacrt/Objavljeno); `published_at` nullable; `objects` default + `published` PublishedManager; `__str__`; `Meta` verbose_name(pun dijakritik)/ordering/indexes. NEMA cross-app FK osim AUTH_USER_MODEL.
- **get_absolute_url (AC1, SM-D6):** poziva `reverse("blog:detail", ...)` → `NoReverseMatch` u 5-1 (assert raises; potvrđuje reverse-based, NE hardkodovan `/blog/...` string). 5.3 ažurira na stvarni `/sr/blog/<slug>/`.
- **PublishedManager (AC3):** DRAFT → NIJE u published; PUBLISHED+past published_at → JESTE; PUBLISHED+published_at=None → NIJE; PUBLISHED+future published_at → NIJE; `Post.objects.all()` vraća SVE; `timezone.now()` per-query (freeze/advance ili dva poziva). **DB-vrednost lock (IMP-3):** `Post.Status.PUBLISHED.value == "published"` (zaključava string na koji manager literal cilja). **Gotcha BL-3:** `Post._default_manager` je `objects` (sav content), NE `published`.
- **Translation (AC4):** Post/Category/Tag u modeltranslation translator; `_sr/_hu/_en` virtuelna polja postoje (title/perex/body/name/description); `slug` NIJE translatable; sr fallback (pristup bez language context → sr).
- **Migracija (AC2):** 0001 CreateModel Category+Tag+Post (**ovim redosledom — Category/Tag PRE Post; IMP-6**); M2M `post_tags` through; `_sr/_hu/_en` kolone za translatable polja; index `blog_post_status_pub_idx`; `status max_length>=9`; `author` swappable_dependency. **Reverz: PREFERIRAJ introspekciju operations liste (IMP-7) NAD round-trip `migrate blog zero` u pytest sesiji** (round-trip može poremetiti deljeno test-DB stanje; ako se koristi → DB izolacija). **REGRESSION:** `makemigrations --check` → no changes van blog/0001.
- **App scaffold (AC5):** `BlogConfig.name=="apps.blog"`; u INSTALLED_APPS POSLE modeltranslation; dep boundary (blog ne importuje products/brands/search/pages/forms — import-graph/AST/modul-level assert).
- **Admin (AC6):** Post/Category/Tag registrovani; `list_display`/`list_filter`/`search_fields`/`prepopulated_fields` definisani; **`PostAdmin.view_on_site is False` (IMP-1)**; superuser GET `reverse("admin:blog_post_changelist")`/`_category_`/`_tag_` → 200 (NIKAD hardkodovan put); **smoke NE sme triggerovati `Post.get_absolute_url` (IMP-1 — inače `NoReverseMatch`/500)**. **Gotcha BL-2 (modeltranslation fragilnost — IMP-2):** `prepopulated_fields`/`search_fields` na base imenima mogu baciti `admin.E030`/`FieldError` → opcije: `TranslationAdmin`, sr-suffiksovane kolone (`title_sr`...), ili ukloni/suzi i defer za 8.7. Admin je MINIMALAN stub koji PROĐE `check` — NE „mirror products" (products admin je BARE register).
- **TEA policy:** `@pytest.mark.django_db` na svaki DB test; pytest-django (NE unittest.TestCase; project-context.md:233). Test useri za `author` kroz `django.contrib.auth.get_user_model()` (NE direktan import). modeltranslation testovi mogu zahtevati `translation.activate("sr")` / context. **factory_boy NIJE u dep** (verifikovano live — mirror 4-1; inline `Model.objects.create(...)`/fixtures, NEMA `factories.py` osim ako TEA odluči i `uv add --dev` — opciono, NE blokira). **Migracioni test: PREFERIRAJ introspekciju `0001` operations liste (IMP-7) NAD `call_command('migrate','blog','zero')` round-trip-om** (round-trip može poremetiti deljeno test-DB stanje; ako se koristi → DB izolacija).

## Risk-Tier Self-Assessment

**TIER: HIGH.**

**Obrazloženje:** PRVA story Epic 5 koja kreira NOVI app + model foundation za ceo blog domen. Kombinuje schema + new-app + cross-cutting rizik faktore:
1. **CreateModel schema migracija (×3 + M2M through + modeltranslation kolone)** — irreverzibilna DB šema; M2M `post_tags` through-tabela + `_sr/_hu/_en` kolone za 6 translatable polja (title/perex/body/name/description) → najveća pojedinačna schema promena u jednoj migraciji do sada; pogrešan `status` choice ugovor (`"published"`) lomi PublishedManager + sve nizvodne (5.2/5.3/5.4) query-je.
2. **FK na `settings.AUTH_USER_MODEL`** — PRVI author FK u projektu; `swappable_dependency` u migraciji; pogrešan `on_delete` (CASCADE bi obrisao objavljen content pri brisanju user-a — kritičan content-integritet rizik; SET_NULL je svesna odluka, SM-D3); direktan `User` import bi prekršio Epic 8.2 RBAC custom-user kompatibilnost.
3. **modeltranslation schema** — prve blog `_sr/_hu/_en` kolone; pogrešna translation.py registracija (npr. slug translatable greškom) ili reg order (apps.blog pre modeltranslation) → admin/migracioni problemi; modeltranslation+prepopulated_fields admin interakcija (Gotcha BL-2).
4. **NOVI app + INSTALLED_APPS + dep boundary** — pogrešan import (blog→products FK) ili reg redosled (pre modeltranslation) uvodi suptilne probleme; manager redosled (Gotcha BL-3) može tiho prebiti default_manager → filtrirani admin (BUG).
5. **PublishedManager import-order (Gotcha BL-1)** — managers↔models kružna zavisnost ako se loše napiše.

NIJE MEDIUM: schema migracija (3 modela + M2M + 6×3 translation kolona) + AUTH_USER_MODEL FK + modeltranslation + novi app boundary + manager edge-case-ovi zajedno prelaze prag. Mitigacija: base klase REUSE (SluggedModel/TimestampedModel dokazane u 2-1/2-2); slug auto-gen pattern kopiran iz Product; admin je STUB (mali); translation mirror products; NEMA data seed (prazan start); NEMA views/urls (čista model story); NEMA novog dep-a (custom Tag, plain body TextField). Gotchas (BL-1..BL-4) eksplicitno dokumentovani.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
