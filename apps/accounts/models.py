"""apps.accounts — prazan models modul (Story 8.1, SM-D3).

V1 NEMA custom User / proxy model (default `auth.User` ostaje; email-as-username
je forma-layer u forms.py, NE custom `AUTH_USER_MODEL`). Modul postoji radi app
loading-a; apps.accounts NE generiše schema migraciju.
"""
