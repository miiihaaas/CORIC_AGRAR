"""Story 8.3 — apps.admin_ext NEMA modela (SM-D3).

admin_ext je čist admin-customization sloj (dashboard override + read-only
agregaciona statistika). Nema state-a → 0 migracija. `makemigrations --check
admin_ext` → „No changes". Fajl namerno prazan (samo docstring).
"""
